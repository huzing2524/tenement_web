<view class='container'>
  <map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}"  show-location='true'></map>
</view>
<view class='nav'>
  <view bindtap='handlepublish'>发布</view>
  <view bindtap='handlesearch'>搜索</view>
</view>
